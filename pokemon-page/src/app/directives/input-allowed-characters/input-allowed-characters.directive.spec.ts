import { InputAllowedCharactersDirective } from "./input-allowed-characters.directive";

describe('InputAllowedCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAllowedCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
